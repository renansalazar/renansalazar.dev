---
const {heroImage, pubDate, title, slug} = Astro.props
---

<a
  class="hover:bg-slate-500 hover:text-slate-100 block rounded-lg p-4 border-solid border-green-700 border duration-100 h-full flex flex-col" 
  href={"/blog/" + slug}>
  <div class="flex-1">
    <img src={heroImage} class="w-full h-48 object-cover rounded-md" />
    <time class="text-sm text-gray-500 block mt-3">{pubDate}</time>
    <h3 class="font-medium mt-2 text-lg">{title}</h3>
  </div>
</a>